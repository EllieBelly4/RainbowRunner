package pathfinding

import (
	"RainbowRunner/internal/types"
)

func LoadPathMap(zoneName string) *types.PathMap {
	//filePath := filepath.Join("data", "pathmaps", strings.ToLower(zoneName)+"_pathmap.json")
	//
	//if stat, ok
	//
	//	file, err :=

	return nil
}
